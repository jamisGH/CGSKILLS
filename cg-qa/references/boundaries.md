# 權責與停止條件

需要決定是否修正、派回或阻擋發布時讀取本檔。

- `cg-qa` 擁有驗收判定，不擁有產品主資料、客戶主檔、FIT 結論、提案策略或 Word/PDF 內容。
- 可指出最小修正條件，不可為了讓結果 PASS 而靜默改寫責任 Skill 產出。
- 不用推測補齊缺少的來源、核准、法規、產品、客戶或商務資訊。
- 嚴重缺陷或已證實不一致為 `FAIL`；必要證據不可取得、來源衝突或驗證工具不可用為 `BLOCKED`。
- 只有所有必要檢查通過且無未解決的重大／關鍵問題才能 `PASS`。

若使用者另行授權修正，仍須交由責任 Skill 執行後重新完整驗收。
